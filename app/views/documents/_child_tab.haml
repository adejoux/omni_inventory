%table.table.table-bordered.table-striped
  %thead
    %tr
      - @headers.each do |header|
        %th=header 
  %tbody
    - @response.each  do |hit|
      %tr
        - @headers.each do |key|
          %td=hit._source[key]
= paginate @response, remote: true
